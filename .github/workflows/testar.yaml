name: Testar
on:
  push:
    branches: 
      - main
  pull_request:
    branches:
      - main
jobs:
  testar:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v27
      - run: nix develop --command npm ci
      - run: nix develop --command npm test
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-results
          path: test-results/
          retention-days: 7
      - run: nix run github:angelonuffer/testes-caixa-preta/80370b7c00b924bcd3177a6299e186f09afa187b
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: telas
          path: testes/telas/
          retention-days: 7
