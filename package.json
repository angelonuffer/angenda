{
  "name": "angenda",
  "version": "1.0.0",
  "description": "Angenda - Gerenciador de Tarefas em Elm, Tailwind CSS e IndexedDB",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "test": "npx playwright install chromium --with-deps && npx playwright test",
    "format": "echo 'No formatting required'",
    "lint": "echo 'No linting required'"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "elm": "^0.19.2-0",
    "playwright": "^1.50.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/angelonuffer/angenda.git"
  },
  "keywords": [
    "elm",
    "spa",
    "indexeddb",
    "tasks"
  ],
  "author": "",
  "license": "ISC"
}
