{
  "name": "angenda",
  "version": "1.0.0",
  "description": "Angenda - Gerenciador de Tarefas",
  "scripts": {
    "build": "mkdir -p alvo && npx elm make fonte/Main.elm --output=alvo/elm.js && cp fonte/index.html alvo/index.html && cp fonte/brand-icon.png alvo/brand-icon.png",
    "test": "npm run build && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "elm": "^0.19.2-0",
    "playwright": "^1.50.0",
    "serve": "^14.2.6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/angelonuffer/angenda.git"
  },
  "keywords": [
    "elm",
    "spa",
    "indexeddb",
    "tasks"
  ]
}
